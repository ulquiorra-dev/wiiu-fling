build-wut-osx:
    stage: toolchain
    script:
        - cd $CI_PROJECT_DIR/packages/wut-osx
        - chmod -R 0777 .
#       Set up a builduser, because makepkg -_-
        - useradd builduser -m
        - passwd -d builduser
        - echo "builduser ALL=(ALL) ALL" | tee -a /etc/sudoers
#       Build!
        - sudo -E -u builduser makepkg -s --noconfirm
    artifacts:
        paths:
            - 'packages/wut-osx/*.pkg*'
#    only:
#        changes:
#            - 'packages/wut-osx/PKGBUILD'
#            - 'packages/wut-osx/ci.yml'
#        variables:
#            - $TRY_CACHE == "yes"
