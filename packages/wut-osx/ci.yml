wut-osx:
    stage: toolchain
    script:
        - *buildsetup
        - *buildscript
    artifacts:
        paths:
            - 'packages/wut-osx/*.pkg*'
#    only:
#        changes:
#            - 'packages/wut-osx/PKGBUILD'
#            - 'packages/wut-osx/ci.yml'
#        variables:
#            - $TRY_CACHE == "yes"
