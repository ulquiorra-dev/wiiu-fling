sdl2:
    stage: libraries
    script:
        - *buildsetup
        - export WUT_ROOT=/opt/devkitpro/wut
        - export PATH="$PATH:/opt/devkitpro/devkitPPC/bin"
        - *buildscript
    artifacts:
        paths:
            - 'packages/sdl2/*.pkg*'
