libdynamiclibs:
    stage: libraries
    script:
        - *buildsetup
        - >
            pacman --noconfirm -U
            $CI_PROJECT_DIR/packages/curl-headers/*.pkg*

        - *buildscript
    artifacts:
        paths:
            - 'packages/libdynamiclibs/*.pkg*'
