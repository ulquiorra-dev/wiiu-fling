sdl2_gfx:
    stage: libraries2
    script:
        - *buildsetup
        - >
            pacman --noconfirm -U
            $CI_PROJECT_DIR/packages/sdl2/*.pkg*

        - *buildscript
    artifacts:
        paths:
            - 'packages/sdl2_gfx/*.pkg*'
