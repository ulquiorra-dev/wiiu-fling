build-sdl2-gfx:
    stage: libraries2
    script:
        - >
            pacman --noconfirm -U
            packages/wut-linux/*.pkg*
            packages/sdl2/*.pkg*
            packages/pkg-config/*.pkg*

        - source /etc/profile.d/*
        - cd $CI_PROJECT_DIR/packages/sdl2_gfx
        - chmod -R 0777 .
#       Set up a builduser, because makepkg -_-
        - useradd builduser -m
        - passwd -d builduser
        - echo "builduser ALL=(ALL) ALL" | tee -a /etc/sudoers
#       Build!
        - sudo -E -u builduser makepkg -s --noconfirm
    artifacts:
        paths:
            - 'packages/sdl2_gfx/*.pkg*'
#    only:
#        changes:
#            - 'packages/sdl2_gfx/PKGBUILD'
#            - 'packages/sdl2_gfx/ci.yml'
#        variables:
#            - $TRY_CACHE == "yes"
