wut-linux:
    stage: toolchain
    script:
        - cd $CI_PROJECT_DIR/packages/wut-linux
        - chmod -R 0777 .
#       Set up a builduser, because makepkg -_-
        - useradd builduser -m
        - passwd -d builduser
        - echo "builduser ALL=(ALL) ALL" | tee -a /etc/sudoers
#       Build!
        - sudo -E -u builduser makepkg -s --noconfirm
    artifacts:
        paths:
            - 'packages/wut-linux/*.pkg*'
#    only:
#        changes:
#            - 'packages/wut-linux/PKGBUILD'
#            - 'packages/wut-linux/ci.yml'
#        variables:
#            - $TRY_CACHE == "yes"
