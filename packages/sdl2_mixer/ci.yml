sdl2_mixer:
    stage: libraries2
    extends: .build
    before_script:
        - >
            pacman --noconfirm -U
            $CI_PROJECT_DIR/packages/sdl2/*.pkg*
    artifacts:
        paths:
            - 'packages/sdl2_mixer/*.pkg*'
