build-pkgconf:
    stage: toolchain
    script:
        - cd $CI_PROJECT_DIR/packages/pkgconf
        - chmod -R 0777 .
#       Set up a builduser, because makepkg -_-
        - useradd builduser -m
        - passwd -d builduser
        - echo "builduser ALL=(ALL) ALL" | tee -a /etc/sudoers
#       Build!
        - sudo -E -u builduser makepkg -s --noconfirm
    artifacts:
        paths:
            - 'packages/pkgconf/*.pkg*'
#    only:
#        changes:
#            - 'packages/pkgconf/PKGBUILD'
#            - 'packages/pkgconf/ci.yml'
#        variables:
#            - $TRY_CACHE == "yes"
