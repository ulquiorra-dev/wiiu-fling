sdl2-image:
    stage: libraries2
    extends: .build
    before_script:
        - >
            pacman --noconfirm -U
            $CI_PROJECT_DIR/packages/sdl2/*.pkg*
    artifacts:
        paths:
            - 'packages/sdl2-image/*.pkg*'
