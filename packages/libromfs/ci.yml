libromfs:
    stage: libraries
    script:
        - *buildsetup
        - export WUT_ROOT=/opt/devkitpro/wut
        - *buildscript
    artifacts:
        paths:
            - 'packages/libromfs/*.pkg*'
