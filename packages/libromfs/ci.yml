libromfs:
    stage: libraries
    extends: .build
    dependencies: []
    before_script:
        - export WUT_ROOT=/opt/devkitpro/wut
    artifacts:
        paths:
            - 'packages/libromfs/*.pkg*'
